# Demo dataset

Reserved for the bundled demo processes (`*.json` files plus an `index.json`).
This is the only sample content shipped with the public release and must contain
**no real or personal data**.

Load it from the app via "Load folder" (webkitdirectory), or copy its files into
`../processes/` to have them picked up directly.
